# Frontend Feature Modules

Add user-facing product domains here when features are built.

Recommended structure:

```text
features/
  mock-tests/
    components/
    hooks/
    pages/
    services/
  practice-sets/
    components/
    hooks/
    pages/
    services/
```

Keep shared visual primitives in `src/components` and feature-specific UI inside the feature folder.
